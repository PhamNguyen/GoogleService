<UserControl x:Class="GoogleServices.LoginButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="70" d:DesignWidth="250">
    
    <Grid x:Name="LayoutRoot" Background="Transparent" Tap="LoginButton_OnTap">
        <Border x:Name="BackgroundBorder" Background="OrangeRed">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" x:Name="ButtonImage" Stretch="Uniform"
                   Height="40" Width="40" VerticalAlignment="Center" Margin="5"/>

                <TextBlock Grid.Column="1" x:Name="ButtonContentTextBlock" VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>